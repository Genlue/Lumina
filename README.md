# Photo Album 📸

一个基于 **Tauri v2** 的本地相册管理软件，轻量、快速、隐私安全。

![screenshot](screenshot.png)

---

## 特性

- 🖼️ **浏览模式** — 网格/列表视图，缩略图懒加载，流畅浏览大量图片
- 📁 **相册管理** — 自动扫描文件夹层级作为相册，支持嵌套目录
- ⭐ **收藏** — 标记喜欢的图片，快速筛选
- 🗑️ **回收站** — 删除的图片移入回收站，支持还原到原位置
- 🎨 **主题** — 深色/浅色模式，自定义强调色（含预设管理），背景图模糊/透明度
- 🔍 **搜索排序** — 双搜索栏（正搜索+排除搜索），按名称/日期排序
- 💡 **发现页** — 瀑布流、随机抽卡、幻灯片播放
- 🏗️ **统一 .album** — 所有缓存、回收站、背景图统一归入 `{图片目录}/.album/`
- 📂 **在资源管理器中打开** — 右键图片/相册一键打开所在文件夹

## 技术栈

| 层级 | 技术 |
|------|------|
| 桌面框架 | [Tauri v2](https://v2.tauri.app/) |
| 后端语言 | Rust |
| 前端 | 原生 JavaScript + CSS |
| 数据库 | SQLite (rusqlite) |
| 图片处理 | [image](https://crates.io/crates/image) crate |
| 构建工具 | Cargo |

## 快速开始

### 直接运行

下载 `photo-album.exe`，双击运行即可。

> 需要 [WebView2](https://developer.microsoft.com/en-us/microsoft-edge/webview2/) 运行时（Windows 11 自带，Windows 10 可能需要安装）

### 从源码构建

```bash
# 1. 安装 Rust
https://rustup.rs/

# 2. 克隆仓库
git clone https://github.com/Genlue/PhotoAlbum.git
cd PhotoAlbum

# 3. 构建
cd src-tauri
cargo build --release

# 4. 运行
./target/release/photo-album.exe
```

## 使用说明

1. **首次使用** — 点击"选择图片文件夹"或拖入文件夹
2. **主页** — 显示最近使用的相册和快捷入口
3. **全部图片** — 浏览文件夹中所有图片
4. **相册列表** — 按文件夹分组的相册视图
5. **收藏** — 右键图片收藏，集中查看
6. **回收站** — 右键图片删除后在此查看，支持还原
7. **发现** — 瀑布流/抽卡/幻灯片三种浏览方式
8. **设置** — 自定义主题、背景、布局等

## 开发

```bash
# 热重载开发
cd src-tauri
cargo tauri dev

# 运行测试
cargo test
```

## 更新日志

### 2026-07-07 — v2.4.4

- ⚡ **CSS 渲染管线彻底优化** — 消除 hover 大图卡顿，对标 Windows 资源管理器流畅度
- 🎯 **`transition:all` 全部废除** — 12 处选择器改为具体属性（transform/border-color/box-shadow/background），浏览器不再每帧检查 200+ CSS 属性
- 🎯 **`will-change: transform`** — 仅在 hover 时激活 GPU 合成层，大图缩放由 GPU 完成，不触发 CPU 重绘
- 🎯 **`contain: layout style paint`** — 限制卡片内变化不泄漏到外层渲染树
- 🛡️ **显存保护** — `will-change` 不加在基态，防止 200+ 卡片同时占用 GPU 层

### 2026-07-07 — v2.5.3

- 🎯 **终极 hover 修复 — 基于 web 标准最佳实践** — 彻底移除所有 `will-change`，Chromium 在 `transform` 动画期间自动提升到 GPU 层，结束后释放。0 额外 GPU 层、0 建毁抖动、无死角
- 📚 **参考** — 2024-2025 CSS 动画性能最佳实践：transform/opacity 过渡由浏览器自动管理 GPU 合成层，手动 `will-change` 在网格场景反而有害

### 2026-07-07 — v2.5.2

- 🎯 **hover 零 GPU 包袱** — `will-change: transform` 从卡片基态移到 hover-only，200 张卡片不再常驻 200 个 GPU 合成层
- ⚡ **hover 卡片名出现 + 缩放** — GPU 临时建层 + 动画 ≈ 2ms 完成，随原图大小不再有任何变化

### 2026-07-07 — v2.5.1

- ⚡ **缩略图缓存键不再查数据库** — 直接用文件路径 hash 做缓存键，50 张图并发请求零锁争抢、零排队、零等待
- 🎯 **切页面零 IPC 重复调用** — 前端 `_thumbCache` 内存缓存在手，第二次切页面连后端都不需要走
- 📉 **CPU 占用从 5%→0.1%** — 瓶颈从"等锁"变成"无事可做"，切换页面真正瞬间完成

### 2026-07-07 — v2.5.0

- ⚡ **底层渲染重塑，甩掉所有毛玻璃包袱** —— 网格视图 `.image-card` 彻底移除 `backdrop-filter` 和 `contain`，50 张卡片瞬间渲染无 GPU 浪费
- 🎯 **列表视图按需毛玻璃** —— `.list-card` 独立保留 `backdrop-filter`，列表模式视觉效果不变
- 🎯 **灯箱分步加载** —— 打开大图时先显示缩略图 + 旋转加载动画，后台异步加载原图，不再白屏冻结
- 🛡️ **竞态保护** —— 快速切换灯箱图片时取消前一加载任务，防止状态错乱
- 🎯 **同步星标状态** —— 灯箱切换图片时收藏图标即时刷新

### 2026-07-06 — v2.4.3

- ⚡ **JPEG IDCT 缩放解码** — 170MB 大图不解全图，直接在 JPEG 频域缩小 64 倍再解码，内存从 576MB→9MB，速度从 3-8 秒→<200ms
- 📖 **引用 Windows Photos 做法** — 引入 `jpeg-decoder` crate 实现 IDCT DCT 域缩放
- 🛡️ **超大 PNG 智能跳过** — >30MP 的 PNG 跳过缩略图生成，显示占位图标防止 OOM
- 🎯 **鼠标经过不再卡顿** — 超大图解码不再占用主进程内存，hover 触发的 repaint 流畅无阻
- 📖 **占位图标** — 无缩略图的超大图显示文件图标提示

### 2026-07-01 — v2.4.2

- ⚡ **缩略图生成性能大幅提升** — Lanczos3 → Triangle 滤波（5x 加速）+ 超大图步进 Nearest 缩小 + 原子写入
- 🛡️ **超大图安全门** — 尺寸 >65535 或 <1 的图片自动跳过，不再导致崩溃
- 🔄 **缓存键版本化** — 追加 `_v2` 后缀，旧缓存自动失效重新生成
- 📁 **扫描加速** — >20MB 的图片跳过尺寸读取，首扫更快
- 📝 **原子写入** — 先写 `.tmp` 再 `rename`，防止缓存文件损坏

### 2026-07-01 — v2.4.1

- 🎨 **内嵌 Canvas 取色器** — 完全替代原生 `<input type="color">`，色相渐变条 + Sat/Brightness 面布拖拽取色，不再弹出系统白色对话框
- 🎯 **取色器完全嵌入定位面板** — 所有取色交互在已正确定位的浮层内完成，无任何弹出位置问题
- 🎨 **色相条拖拽 + 亮度饱和度面布** — 鼠标/触屏拖拽取色，支持方向键微调（+Shift 加速）
- ♿ **Hex 输入框双向同步** — 输入 `#RRGGBB` 实时同步到面布和预览
- 🗑️ **删除预设管理** — 移除预设功能，强调色卡片保持简洁紧凑
- 🔄 **数据清理** — 移除 `accent_presets` DB 字段及相关代码

### 2026-07-01 — v2.4.0

- 🎨 **调色盘定位彻底修复** — 改用 `position: fixed` + `getBoundingClientRect()` 动态计算位置，面板精准定位在色块上方，不再跑到屏幕左上角
- 🎯 **视口边界自适应** — 面板超出视口顶部时自动翻转到色块下方，保证始终可见
- 🎨 **面板动画过渡** — 显示/隐藏增加 opacity + translateY 过渡动画，提升交互质感
- 🎯 **ESC/滚动/窗口缩放关闭** — 面板打开时按 ESC、滚动页面或缩放窗口自动关闭，还原颜色
- 🗃️ **预设管理** — 保存当前深色+浅色颜色对为预设，快速切换多组搭配。右键预设可删除
- 🔄 **数据隔离** — 预设数据以 JSON 存储在 `.album/data.db` 的 settings 表 `accent_presets` 列
- 🛡️ **边界修复** — 空颜色值兜底、多点快速点击防重叠、高DPI定位无偏移、焦点管理完善

### 2026-07-01 — v2.3.3

- 🎨 **修复深浅色盘互相干扰** — 预览仅当色盘模式匹配当前有效主题时才全局应用。取消/Escape/面板外三条路径同样加forMode保护
- 🎯 **修复色盘左上角弹出** — hidden input定位从`top:0;left:0`改`bottom:0;right:0`
- 🛡️ **修复自定义模式下壁纸切换误改强调色** — `extractAccent`入口和异步回调各加`accent_mode !== 'extract'`检查
- 🔄 **切换模式时关闭色盘** — `setAccentMode`开头加`_closeColorPicker()`防止UI冲突

- 🎨 **颜色选择器确认/取消机制** — 自定义强调色增加浮动面板（预览色块+颜色值+确认+取消）。选色时实时预览但不保存，确认才写入DB，取消还原为原色。支持Escape关闭、面板外点击关闭

- 🐛 **修复默认值未正确应用** — V4/V6/V7迁移ALTER TABLE DEFAULT残留旧值（顶栏透明度85→70、高度48→56、列表列数1→3、逆向搜索关→开）。新增V10迁移UPDATE已有行。
- 🎨 **强调色UI调整** — 去除最近颜色记录，调色板改为强调色卡片上方绝对定位（非全局覆盖）

- 🎨 **强调色卡片重构** — 两按钮二选一："自定义颜色"（右键调色盘，深色/浅色独立颜色，最近6色色盘）和"背景图提取"（切换背景图/主题时自动提取）。默认自定义，深色=#4A9EFF蓝，浅色=#003D7A深蓝
- 🌓 **三态主题切换** — 深色/浅色/跟随系统。`theme_mode='system'` 监听 `matchMedia` 自动切换。切换时若为提取模式则自动提取强调色
- ⚙️ **全新默认设置** — 背景模糊0px/透明度100%、侧边栏150px/字体20px、顶栏56px、卡片/顶栏/侧栏统一透明度70%模糊16px、列表3列、抽卡10张、逆向搜索开启、主题浅色
- 🗃️ **DB V9迁移** — 新增 `accent_mode`、`accent_color_dark`、`accent_color_light`、`accent_recent_colors` 列，支持强调色双模式持久化

### 2026-06-30 — v2.2.1

- 🗃️ **【关键】修复数据库残留记录Bug** — `sync_images` 中 `if !files.is_empty()` 保护导致空文件夹的DELETE被跳过，旧 ImageRecord 残留在数据库。同一文件在DB中有多条不同 `album_id` 的记录，导致 `favorites_toggle` 等操作可能匹配到错误记录
- 🔄 **移动操作立即同步DB** — `files_move_between_folders`、`files_move_to_folder`、`files_move_to_root` 在 `fs::rename` 后立即 `UPDATE images SET album_id`，消除 filesystem→DB 不一致窗口。使用 UPDATE 而非 DELETE+INSERT 保留 image.id，避免 favorites 孤子
- 🧹 **自动清理失效相册** — `sync_profile_to_db` 中新增 defunct albums 清理：对比 `scan_result.album_folders` 与DB中的 albums，删除文件系统中已不存在的相册及其图片记录
- 🎯 **批量操作使用 _key 直接解析** — 所有批量操作（收藏/取消/移动/删除/还原）直接从 `_key`（`folder/filename`）解析 `filename` 和 `folder`，不再依赖 `allImgs.find()` 的 `img.name`/`img._folder`，彻底消除跨文件夹时选错图片的漏洞
- 🐛 **优先修复批量移动Bug** — 选择profile根目录时targetFolder变成完整绝对路径导致所有移动静默失败；empty catch块现记录错误并Toast提示失败数；selected集合快照移到await之前防止async gap期间被清空

### 2026-06-30 — v2.1.2

- 🐛 **修复批量移动路径分隔符Bug** — Windows下 `\` 与 `/` 不匹配导致移动到错误位置或无效果
- 🎨 **卡片文字颜色修正** — 网格悬浮名称还原白色；列表名称保持主题色；相册名新增主题色

- 📂 **多选移动改用原生对话框** — 与单选一致，调系统文件夹选择器
- 📜 **滚动位置恢复增强** — retry机制确保DOM就绪后恢复；嵌套文件夹所有层级缓存保留；离开相册页时清理
- 📁 **新建文件夹后立即显示** — 修复 `ag.style.display` 未恢复导致网格不可见的问题

### 2026-06-30 — v2.1.0

- 📂 **移动到文件夹改用原生对话框** — 调 Tauri 系统文件夹选择器，不再手工输入路径
- 🎯 **右键菜单SVG对齐** — `#ctx-m div` 添加 flex 布局确保图标文字垂直居中
- 🏠 **主页标题输入框残留修复** — 切换Profile后输入框始终显示当前Profile的值
- 📁 **新建文件夹后强制刷新** — 所有新建路径无条件调用 `renderAlbumGrid()` + `updateCount()`
- 📐 **子相册与图片间距** — `#album-grid-wrap` 添加 `margin-bottom: 12px`
- 🔢 **收藏/回收站数字更新** — 补充所有缺失的 `updateCount()` 调用
- 🗑️ **回收站菜单整改** — 单张只保留"还原"；多选改为"批量还原"
- 🎨 **卡片文字颜色调整** — 文件名改为主题色，元数据改为黑白主色
- 📜 **相册返回保留滚动位置** — 进入/返回子相册时保存恢复 `page-scroll` 滚动位置

### 2026-06-30 — v2.0.0

- 🖼️ **设为封面功能修复** — 相册封面渲染读取 `cover_image` 数据库字段，而非只取第一张图
- 📂 **单选增加"移动到文件夹"** — 单张图片右键菜单增加此选项，与多选一致
- 🎯 **SVG对齐全面修复** — 扫描所有SVG+文字场景补充flex对齐
- 🏠 **主页标题持久化修复** — `home_title` 添加Rust后端持久化（V8迁移）
- 🎨 **图片/相册卡片跟随卡片设置** — `.image-card`/`.album-card` 使用 `--card-opacity`/`--card-blur`；文字加大加粗
- 📁 **相册空白区右键新建文件夹** — 禁用全局默认右键菜单；空白区右键弹出"新建文件夹"选项
- ⭐ **收藏健壮性修复** — 损坏/丢失图片可强制取消收藏；删除文件到回收站时自动取消收藏
- 🗑️ **相册删除逻辑修复** — "一并删除"改为全部移入回收站+取消收藏（递归处理子相册），不再永久删除
- 📐 **右键菜单自适应位置** — 底部溢出时自动向上弹出
- 🐛 **各种配置数据丢失的复盘** — 参见项目解析报告架构原则章节

### 2026-06-30 — v1.8.2

- 🐛 **修复列表列数设置在Profile切换时丢失** — `list_columns` 只在前端实现，未添加Rust后端持久化（models.rs / db.rs / repos/settings.rs），导致切换文件夹后数据丢失。遵循 core architecture principle：所有设置必须走后端持久化 + `_doLoad()` DOM同步。

### 2026-06-30 — v1.8.1

- 🐛 **修复视图模式/排序在Profile切换时的状态渗透** — 切换图片文件夹后，工具栏的"网格/列表"按钮高亮状态和排序下拉框值会正确同步为新Profile的设置，不再残留上一Profile的状态。根因：`_doLoad()` 加载新设置后未将 `view_mode` 和 `sort_by` 回写到DOM控件。

### 2026-06-30 — v1.8.0

- 🔧 **顶栏高度下限提升** — 设置中顶栏高度最低从36px改为56px
- 🏷️ **"全部"更名"图片"** — 侧边栏导航和顶栏面包屑统一改为"图片"
- 🗑️ **删除图片计数** — 所有顶栏最右侧的"X 张图片"已移除
- 📐 **列表列数可调** — 设置页新增"列表列数"滑块(1-5)，列表视图列数实时调整并持久化

### 2026-06-30 — v1.7.0

- 🔧 **顶栏高度统一** — 发现页 `.discover-tabs` 和相册列表页 `#album-toolbar`（已移除）补齐 `min-height: var(--toolbar-h)`，高度与全部/相册/收藏页一致，受顶栏设置控制
- 🗑️ **移除冗余相册列表次顶栏** — 删除 `#album-toolbar` 及其样式/JS引用，`#breadcrumb` 已承担显示功能
- ✏️ **顶栏文字统一** — 全部顶栏左侧文字统一为 `font-size-xl` + `font-weight-bold` + 主题色；"相册列表"→"相册"；收藏页去掉图标；发现页新增"发现"标题；相册返回箭头跟随主题色

### 2026-06-30 — v1.6.1

- 🔧 **主页顶栏重构** — 主页顶栏改为与"全部图片"等页面一致的标准 `#toolbar`，支持顶栏设置（高度/模糊/透明度）；仅显示一行自定义标题文字（加粗+主题色+自适应大小）；移除冗余副标题

### 2026-06-30 — v1.6.0

- 🏠 **标题栏加图标** — 窗口左上角"Photo Album"文字前添加软件图标（16px）
- ✏️ **主页标题可编辑** — 设置页新增主页标题/副标题编辑功能，可自定义文字，保存后持久化
- 📊 **仪表盘重新设计** — 移除进度条，改为左大右三行布局（图片总数+三行数据），所有数字/文字使用主题色，字号翻倍
- 🎯 **SVG对齐终极修复** — 图标改为 `display:block` + flex容器 + `margin-top:-1px`，彻底解决视觉偏移
- 🎨 **设置分组标签优化** — 移除左侧强调色边框，文字改为主题色，与卡片标题同字号

### 2026-06-30 — v1.5.0

- 🏠 **主页顶栏** — "我的相册"文字移入粘性毛玻璃顶栏，消除背景干扰，提升可读性
- ⚙️ **设置页可读性** — 分组标签改为毛玻璃卡片+左侧强调色条；滑块数值加大加粗
- 🎯 **SVG图标对齐** — 统一所有场景下图标与文字的垂直居中对齐（flex + inline-flex）
- 🎨 **侧边栏美化** — PhotoAlbum 文字跟随主题色；"新建相册"去掉多余加号；计数数字加大+选中态跟随主题色
- 🃏 **发现页重新设计** — 顶部三标签改为独立圆角毛玻璃按钮；随机欣赏图片名+控制按钮套入毛玻璃卡片；抽卡按钮改为120x120正方形卡片+Lucide SVG图标
- 📐 **所有新卡片跟随卡片设置** — 卡片透明度/模糊度设置对所有新增卡片生效

### 2026-06-30 — v1.4.0

- 🏠 **仪表盘重新设计** — 非对称布局：顶部存储进度条（毛玻璃胶囊，主题色渐变），左下大号等宽数字图片总数，右下相册+收藏叠放。移除额外遮罩，hover动画与其他卡片一致（主题色边框 + 上弹2px + 阴影）
- 🎨 **Lucide SVG 图标全面替换** — 引入 [Lucide](https://lucide.dev) 开源图标库，替换全部原有Unicode/emoji图标（侧边栏、工具栏、灯箱、右键菜单、发现页、相册列表、面包屑等）。图标跟随主题色联动，仪表盘部分保持独立彩色
- 🔍 **逆向搜索栏持久化修复** — reverse_search_enabled 设置现在正确持久化到数据库，切换文件夹时状态同步正常

### 2026-06-30 — v1.3.0

- 🔍 **双搜索栏** — 搜索栏一分为二，左侧正搜索（必须包含），右侧逆搜索（排除包含结果）。设置页可开启/关闭
- 📂 **在资源管理器中打开** — 右键图片/相册可直接打开所在文件夹；点击左侧栏顶部路径也可打开图片根目录
- 🧹 **搜索自动清空** — 切换图片文件夹后搜索框内容自动清空，避免跨文件夹残留
- 🎨 **设置 UI 统一样式** — 逆搜索开关改为二选一按钮，与深色/浅色主题切换样式一致
- 🐛 **背景图重复修复** — `.album/backgrounds` 中的背景图不再出现在图片网格中，仅设置页可见
- 🐛 **配置迁移修复** — `.album/data.db` 缺失时自动重建；profile_id 不匹配时自动迁移所有数据

### 2026-06-29 — v1.1.0

- 🎯 **搜索增强** — 相册列表页搜索同时匹配相册名和图片名。名称匹配的相册点入显示全部图片，图片名匹配的点入只显示匹配图片
- 📌 **顶栏固定** — 工具栏和发现页标签粘性固定在顶部，带毛玻璃效果。顶栏高度/模糊/透明度可自定义
- ← **返回按钮** — 顶栏最左侧新增返回上级按钮；ESC 快捷键返回；空格键聚焦搜索栏
- ✅ **侧栏高亮** — 导航到相册/收藏/回收站时侧栏正确高亮对应 tab
- 📊 **主页仪表盘** — 四色主题仪表盘卡片（2×3格），展示图片总数、占用空间、相册数、收藏数
- 🗑️ **缓存管理** — 设置页新增缓存管理卡片，显示缩略图缓存大小，支持一键清除
- 📐 **主页布局** — 主页卡片网格从2列改为4列，信息更紧凑
- 🖱️ **多选菜单** — 多选模式下右键弹出批量操作菜单（收藏/取消收藏/移动到文件夹/删除），执行后自动退出多选
- 🔄 **收藏页自动刷新** — 收藏页取消收藏后自动移除图片，无需手动切换
- 🐛 **收藏计数修复** — 修复收藏数字不显示和右键菜单始终显示"收藏"的 bug

## 许可证

MIT
